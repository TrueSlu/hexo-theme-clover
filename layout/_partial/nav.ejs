<nav id="nav">
  <header class="header">
    <a href="/" class=""><img src="/images/logo.png" alt="Logo" style="height: 32px; width: 140px;"></img></a>
  </header>
  <div class="ctnWrap">
    <div class="icons">
      <% if (theme.social) { %>
        <% _.forEach(theme.social, function(value, key) { %>
          <% if (value) { %>
            <a href="<%= url_for(value) %>" target="_blank" class="nav-icn iconfont icon-<%= key.toLowerCase() %>"></a>
          <% } %>
        <% }); %>
      <% } %>
    </div>
    <div class="menu">
      <% if (theme.menu) { %>
        <% _.forEach(theme.menu, function(value, key) {
          value = value.path || value;
          if (value) { %>
            <a href="<%= url_for(value) %>" class="nav-menu <%= is_current(value) ? 'z-active' : '' %>"><%= key.toUpperCase() %></a>
          <% } %>
        <% }); %>
      <% } %>
    </div>
  </div>
</nav>
